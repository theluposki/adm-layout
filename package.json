{
  "name": "adm-layout",
  "private": true,
  "version": "0.0.15",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm version patch && npm run build &&  gh-pages -d dist --dotfiles",
    "patch": "npm version patch && npm run deploy",
    "minor": "npm version minor && npm run deploy",
    "major": "npm version major && npm run deploy"
  },
  "dependencies": {
    "dexie": "^4.3.0",
    "pinia": "^3.0.4",
    "remixicon": "^4.9.1",
    "vue": "^3.5.25",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.4",
    "@vitejs/plugin-vue": "^6.0.2",
    "gh-pages": "^6.3.0",
    "vite": "^7.3.1",
    "vite-plugin-pwa": "^1.2.0"
  }
}
